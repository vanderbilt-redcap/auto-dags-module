{
	"name": "Auto DAGs",

	"namespace": "Vanderbilt\\AutoDAGsExternalModule",

	"authors": [
		{
			"name": "Mark McEver",
			"email": "mark.mcever@vanderbilt.edu",
			"institution": "Vanderbilt University Medical Center"
		}
	],

	"permissions": [
		"hook_save_record"
	],

	"description": "Automatically creates, renames, and assigns records to Data Access Groups (DAGs) based on a specified Text field, in which the value of the field becomes the name of the DAG. If the DAG already exists, then the record will be assigned to the existing DAG.",

	"project-settings": [
		{
			"key": "dag-field",
			"name": "Field used to generate DAGs",
			"type": "field-list"
		}
	],

	"links": {
		"project": [
			{
				"name": "Set DAG for all records",
				"icon": "report",
				"url": "set-all-dags"
			}
		]
	}
}
